import { StyleSheet } from 'react-native';

import { baseSpace, colors } from '~/styles/variables';

export default StyleSheet.create({
  container: {
    backgroundColor: colors.background,
    flex: 1,
    padding: baseSpace,
  },
});
